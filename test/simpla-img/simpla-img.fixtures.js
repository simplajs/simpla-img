(function() {
  var NAME = 'simpla-img';

  window.fixtures = window.fixtures || {};
  window.fixtures[NAME] = {

    fileEvent: {
      target: {
        files: [{
          src: 'foo'
        }]
      }
    }

  };
})();
